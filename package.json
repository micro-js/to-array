{
  "name": "@f/to-array",
  "description": "Convert to array from array like.",
  "repository": "git://github.com/micro-js/to-array.git",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
